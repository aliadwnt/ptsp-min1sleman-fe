import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // pastikan file CSS sudah diimpor
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
